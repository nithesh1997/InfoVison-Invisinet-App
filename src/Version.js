const version = {
  major: "0",
  minor: "3",
  build: "23",
  type: "alpha", // Should be like alpha, beta, stable, etc.; Shell script will process only alphanumeric value.
};

export default version;
