export const NEW_HANDLER = (
  currentRow,
  PayloadState,
  RunEffectState,
  ButtonStates,
  TableStates,
) => {
  const [payload, setPayload] = PayloadState;
  const [runEffect, setRunEffect] = RunEffectState;
};
